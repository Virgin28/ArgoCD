apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rollout-practice.fullname" . }}-test-connection"
  labels:
    {{- include "rollout-practice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rollout-practice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
