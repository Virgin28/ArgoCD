apiVersion: v1
kind: Service
metadata:
  name: {{ include "rollout-practice.fullname" . }}
  labels:
    {{- include "rollout-practice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rollout-practice.selectorLabels" . | nindent 4 }}
