apiVersion: v1
kind: Service
metadata:
  name: {{ include "version2.fullname" . }}
  labels:
    {{- include "version2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "version2.selectorLabels" . | nindent 4 }}
